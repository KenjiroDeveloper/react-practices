export const Button = ({handleClick}) =>{

    return (
        <button className="bg-indigo-900 w-full h-full rounded-2xl"
        onClick={handleClick}>
            Hola pibe
        </button>
    );
}